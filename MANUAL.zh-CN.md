# PiHub 使用手册

*你的π，由此汇聚 — pi coding agent 的本地 Web 面板。*

[English](MANUAL.md) · **中文版**

> **安全优先：** PiHub 仅监听 `127.0.0.1` / `localhost`，从不读取
> `~/.pi/agent/auth.json`。你所见的一切都在本机。
> 可选的局域网访问默认关闭——见第 12 章。

---

## 1. 安装与首次运行

环境要求：

- [pi](https://pi.dev) ≥ 0.83（`pi --version`）
- Node.js ≥ 20
- 已配置好的模型渠道（pi 常规配置）

```bash
git clone <仓库>/pi-panel
cd pi-panel
npm install
npm run dev
```

- Web UI：**http://localhost:18384**
- 后端：`127.0.0.1:3001`（页面将 `/api` 代理到它）

打开地址并确认顶栏显示「服务器在线」。面板会自行拉起
`pi --mode rpc`，无需另开终端。

生产构建：`npm run build` → 由 Node 服务托管 `dist/`（`npm start`）。
测试：`npm test`。

> **为什么是 18384 而不是 3001？** 18384 只是 Vite 开发服务器端口（第 14 章：
> `npm run dev` 时界面在 http://localhost:18384，`/api` 代理到后端
> 127.0.0.1:3001）。打包后的 `pihub`/`npm start` 是单进程：Node 服务自行
> 托管界面，默认端口 **3001**。

### 端口

服务器仅监听 `127.0.0.1`。更换端口：

```bash
PIHUB_PORT=4000 pihub        # 打包 CLI
PIHUB_PORT=4000 npm start    # 源码运行
```

`PIHUB_PORT` 优先于通用 `PORT` 环境变量（很多部署平台会自动注入 `PORT`）。
设置页「关于」区始终显示当前实际端口。

### 版本

PiHub 遵循[语义化版本](https://semver.org/lang/zh-CN/)。每次发布的完整
变更记录见 [`CHANGELOG.md`](CHANGELOG.md)；面向用户的 release notes
模板与社区素材在 [`docs/`](docs/release-notes-template.md)。

---

## 2. 界面导览

```
┌─────────────────────────────────────────────────────────────────┐
│ π PiHub · 你的π，由此汇聚             [服务器在线] [☾]           │ 顶栏
├──────────────┬──────────────────────────────────────────────────┤
│ 新建会话/任务 │  ▎标签1 ✕  ▎标签2 ✕  ▎+       （多标签条）       │
│ 搜索         │  ─────────────────────────────────────────────   │
│ / 命令面板   │  工具条（当前会话操作：新建分支/归档）            │
│ ──────────── │  对话流                                           │
│ 集合         │                                                  │
│ 未分组       │  ─────────────────────────────────────────────   │
│  ● 会话列表  │  模型 [▾]  思考 [▾]                              │
│ ──────────── │  输入框 …                    [♡] [发送]          │
│ 帮助 ⏱ ▦ ⚙  │  模型行 · 提示：选择立即生效                      │
└──────────────┴──────────────────────────────────────────────────┘
```

- **顶栏**：品牌、服务器状态、主题切换（☾）、菜单（移动端）。
- **侧边栏**：新建会话、搜索、自动化面板、集合、带状态灯的会话列表、
  底部操作（帮助/历史/统计/设置）。
- **拖拽条**：拖动侧边栏与内容区之间的竖条调整宽度（200–480px，会记住）；
  收起入口在 **设置 → 个性化设置 → 侧边栏**（收起后经左侧窄条展开）。
- **多标签条**：每个打开的会话一个标签（见第 4 章）。当前标签以主色
  下划线标识；点 ✕ 关闭、点 + 新建。
- **工具条（右上）**：当前会话操作——新建分支（克隆）与归档。
- **输入区**：发送/引导按钮、收藏按钮，底部一行是模型与思考档位选择。

### 视图切换（键盘）

| 快捷键 | 动作 |
| --- | --- |
| `Alt+1` | 对话 |
| `Alt+2` | 会话历史 |
| `Alt+3` | 统计与成本 |
| `Alt+4` | 设置 |
| `Alt+5` | 自动化中心 |
| `Esc` | 中断运行中的 agent |
| `Ctrl+Shift+M` | 自动化 / 技能 / 工程流面板 |
| `Ctrl+Shift+L` | 循环切换模型 |
| `⌘`/`Ctrl` + `↑`/`↓` | 切换会话（修饰键在设置中选择） |

---

## 3. 对话

### 发送方式
在 **设置 → 个性化设置 → 发送方式** 中选择：

| 方式 | 行为 |
| --- | --- |
| `回车`（默认） | 回车发送，Shift+回车换行 |
| `⌘+回车` | 仅 Command+回车发送 |
| `Ctrl+回车` | 仅 Ctrl+回车发送 |

agent 运行中输入回车会以 **steer（引导）** 模式注入消息；**Esc** 中断。

### `/` 建议
在输入框输入 `/` 打开自动化/技能/工程流列表：`↑`/`↓` 选择、
`Tab`/`Enter` 应用 `/<name> `、`Esc` 关闭。

### 图片
从剪贴板粘贴图片到输入框，随下一条消息发送。

### 推理界面
- **思考三态**（icon，无 emoji）：*思考中…*（逐字渐变动画）、
  *思考过程…*（默认折叠，点击展开）、*思考被打断！*（红色）。
- **工作流结算**：运行完成后显示 `已消耗 hh时mm分ss秒` + `>`；
  点击 `>` 折叠整个 prompt 工作流；全宽分割线收尾。
- **简化输出**（实验室）：完成的会话自动折叠为结算摘要，
  `.....` 标记被省略的后续内容，点击可展开。

### 分支
从工具条（**新建分支**）或会话右键菜单（**新建分支**）发起：PiHub 先
切换目标会话再克隆（pi 的 clone 分叉当前会话），新分支从你最近一条
用户消息开始，并继承父分支别名（`新支源自{旧 alias}`）。

---

## 4. 多标签工作台

会话以**并行标签页**的形式打开在对话区顶部：

- **打开或切换**：点击侧边栏任一会话，会在新标签中打开；若该会话已在
  某个标签中，则直接切换到那个标签。
- **状态隔离**：每个标签拥有独立的对话流、输入草稿与预览状态。只有
  当前标签是活跃的；切换标签时会重新加载该标签的会话内容。
- **草稿标签**：未绑定的标签（显示「新会话 / New chat」）跟随 RPC 进程
  当前持有的会话——点「新建会话」后它显示新会话，用快捷键切会话后它
  跟随显示当前会话。全工作区最多一个草稿标签。
- **关闭**：点标签上的 ✕ 关闭；关闭当前标签时自动激活相邻标签。
  全部关闭后自动新建一个草稿标签——工作区永远不会没有标签。
- **新建**：点 `+`（或任何入口的「新建会话」）打开草稿标签并开始新会话。

> **关于并行的说明**：pi 的会话池保持单例——同一时刻只有一个会话处于
> 活跃状态。标签是 UI 层并行：激活一个绑定会话的标签时，会先切换 RPC
> 会话再展示其内容。两个标签不会同时向两个会话发送 prompt。

---

## 5. 会话

### 状态灯
每个会话带状态点：🟢 已完成 · 🔵 进行中 · 🔴 已中断。当前会话高亮；
正在等待请求（如待审批对话框）时状态点闪烁。

### 打开与切换
点击会话即在标签中打开（第 4 章）。`⌘`/`Ctrl` + `↑`/`↓` 循环切换会话
（修饰键在设置 → 个性化设置 中选择）。`Alt+2` 打开历史页，`Alt+1`
回到对话。

### 集合
点击「集合」旁的 **＋** 新建，拖拽会话入组；点击集合名可重命名，
垃圾桶图标删除。不在任何集合的会话显示在「未分组」。

### 右键菜单
右键会话行：**打开 · 新建分支（克隆）· 归档 · 删除**。

- *新建分支*：先切换目标会话再克隆（pi 的 clone 分叉当前会话）；
  工具条的克隆按钮对当前会话执行同样操作。
- *归档*：从侧边栏隐藏，可在 **设置 → 会话管理** 恢复。
- *删除*：弹确认框后删除会话文件（服务端防路径穿越，仅允许删除
  sessions 目录下的 `.jsonl` 文件）。

### 详情与树过滤
历史页打开会话可浏览消息树（分支导航）、重命名、统计、压缩与过滤：
**全部 · 主线 · 无工具 · 仅用户**（「已标记」为预留，暂无标签数据）。

### 会话树视图
详情页提供 **树视图** 切换：对话渲染为分支时间线——垂直主线 +
 缩进离支、角色字形（用户/助手/工具/分支）、时间戳与摘要。点击主线
 用户消息上的 fork 图标可**一键分叉**（复用标准分支流程，新分支继承
 传统别名前缀）。

---

## 6. 统计与成本

统计视图（`Alt+3`）基于本地会话记录聚合 token 用量与花费：

- **成本构成**：按模型提供商、按目录的占比横向条
- **按日趋势**：每天的成本柱状（UTC 日界）
- **最贵会话**：花费最高的会话列表（token + 成本）
- **按目录 token 汇总**：输入 / 输出 / 缓存
- 全局与会话内总计（历史页同样可见）

所有图表均为零依赖 SVG——跟随明暗主题渲染，不发任何网络请求；无数据
时显示提示而非空轴。

---

## 7. 文件工作台

agent 工作时，它读取/写入/编辑/修补的文件会被追踪：

- **可点击路径**：工具输出中的文件路径变成链接——点击即预览。
- **只读预览**：浮层展示文件内容。预览仅限当前会话工作目录（白名单
  跟随你所切换的会话；越界 symlink 会被拒绝）。
- **行内 diff**：带 unified diff 的预览会以 +/- 高亮新增/删除行。
- **最近文件条**：最近读 / 写 / 编辑 / 修补调用，去重后一键预览。

工作台只读——PiHub 不会代你写入任何文件。

---

## 8. 自动化中心

自动化中心（`Alt+5` 或侧边栏入口）是内置的工作流面，分三个标签页：

### 技能
完整命令目录——技能、提示词模板与扩展命令，支持搜索与一键运行。
输入框内输入 `/` 实时提供同样的建议。

### 自动化
实时开关，发送到 agent 会话：

- **自动压缩** 与 **自动重试**
- **引导 / 跟进模式**（含并行跟进队列）

### 工程流（Pipelines）
**工程流是 PiHub 独家的多步编排**——pi 本身没有等价面：

- 一个工程流是声明的步骤序列：`prompt` / `steer` / `approval` /
  `setModel` / `setThinking`，在同一 pi 会话上执行
- **匹配分支**：步骤可按 agent 回复分支
- **错误策略**：步骤失败时停止 / 重试 / 继续
- **人工审批门**：带 `requiresApproval` 的步骤会暂停运行并在界面请求
  批准；批准后才会真正发送 prompt
- **实时时间线**：每次运行经 SSE 流式展示逐步骤进度；可随时中断
- **技能导入**：把任意技能转成工程流——算法转换（零 token）或
  agent 辅助转换（消耗 token，运行前确认）

工程流与你的对话共用同一 RPC 会话，因此工程流运行、审批与引导共享
同一条会话。

---

## 9. 模型与渠道

### 模型与思考
输入区底部一行的 **模型** 与 **思考** 选择即时生效（无保存按钮）。
`Ctrl+Shift+L` 循环切换模型。

### 自定义 API 渠道
**设置 → 模型渠道 → 渠道** 编辑 `~/.pi/agent/models.json`：

| 字段 | 含义 |
| --- | --- |
| 渠道名称（key） | `models.json` 中的 provider key |
| Base URL | 端点，如 `https://api.example.com/v1` |
| API Token | 存入 `models.json`（与 pi 约定一致） |
| API 类型 | openai-completions / anthropic-messages / google-generative |
| 模型 ID / 名称 | 模型标识与显示名 |
| 最大上下文 | context window（tokens） |
| 最大输出 | 最大输出 tokens |
| 详细（手风琴） | 支持思考、输入类型 |

点击 **保存** 写入文件。上方「模型仓库」为只读列表（含已配置的成本数据）。

---

## 10. 多 agent 可见性

PiHub 提供**同机其他 agent CLI 的会话只读视图**，无需切换工具即可查找
历史工作：

| 适配器 | 展示内容 | 数据源 |
| --- | --- | --- |
| **Codex** | 会话历史与会话内消息/用量详情（rollout 记录） | `~/.codex/sessions` |
| **AtomCode** | 会话历史，含消息与 datalog 条目 | `~/.atomcode/history.json` |
| **ZCode** | 模型 I/O 记录（用量/耗时/回合 id） | `~/.zcode/cli/rollout/` |

- 每个适配器在会话列表中有**独立主题色**；可在 **设置 → 外观** 覆盖。
- Codex **默认不会被拉起**；可选的 `exec` 集成为 opt-in，以 JSONL 事件
  流接入，不干扰运行中的 Codex。
- 凭据文件（`auth.json`、`auth.toml`）从不读取；被脱敏的字段永不渲染。

---

## 11. 设置

设置视图会将侧边栏切换为导航树，底部 **返回主界面** 按钮回到对话。

| 分区 | 内容 |
| --- | --- |
| **通用设置** | 语言（中文/English）、主题（浅/深）、运行模式（steering/follow-up）、agent 设置 |
| **个性化设置** | 用户 ID、侧边栏展开/收起、发送方式、会话切换修饰键 |
| **模型渠道** | 模型仓库（只读）、自定义渠道编辑器 |
| **会话管理** | 归档会话（恢复）、导出当前会话为 HTML |
| **权限管理** | 浏览器通知状态/请求、本机边界说明 |
| **提示词收藏夹** | 管理收藏提示词，可一键运行 |
| **外观** | 各适配器主题色（pi / Codex / AtomCode / ZCode） |
| **访问模式** | 网络模式展示、配对码生成/撤销、远程能力开关（第 12 章） |
| **实验室能力** | 流式动画、紧凑工具卡片、完成后通知、简化输出 |

### 收藏夹
在输入框旁点击书签按钮收藏当前输入；在 **设置 → 提示词收藏夹**
管理与运行（运行会切回对话并立即发送）。

---

## 12. 隐私、访问与权限

### 默认行为
- 面板仅绑定本机回环地址，并拒绝其他 Host 头。
- 从不读取或暴露 `~/.pi/agent/auth.json`。
- 写入范围有限：经 pi RPC 发起的对话、保存渠道时的 `models.json`、
  浏览器 localStorage 中的面板偏好。
- 浏览器通知只在您主动触发时请求（权限管理区或实验室的完成后通知）。

### 控制令牌
所有写接口与敏感读接口（模型配置 / 文件预览 / 会话状态 / SSE 事件流）
都需要随进程生成的随机**控制令牌**。页面自动获得令牌并在请求中携带
（`X-PiHub-Token` 头；事件流以 `?token=` 传递）。令牌从不落盘、不记
日志。API 响应不会被 Service Worker 缓存（`Cache-Control: no-store`）。

### 局域网访问（默认关闭）
来自其他设备的访问**默认关闭**。显式开启后（`PIHUB_NET=pair` 或
`lan`）：

- **配对码**：一次性短时效代码；远端凭码换取会话令牌。配对码可在
  **设置 → 访问模式** 随时撤销或轮换。
- **能力开关**：每类远程写操作——prompt/steer、删除、shell、审批——
  都有**独立开关，全部默认关闭**。未携带有效配对令牌的远程请求一律
  拒绝（403）。
- 本机回环访问不受这些开关影响。

### 文件预览防越界
预览路径读取前会用 `realpath` 对照真实工作区根二次校验——工作区内指向
外部的 symlink 会被拒绝。

---

## 13. 故障排查

| 现象 | 处理 |
| --- | --- |
| 页面显示「服务器离线」 | 确认后端在运行（`npm run dev`）；检查 `3001` 是否被占用 |
| 18384 端口被占 | 清理残留进程，或修改 `vite.config.ts`（前端）中的端口 |
| 「Agent is already processing」 | 共享的 pi 会话正忙（可能被其他工具占用）；等待或以 steer/follow-up 发送 |
| 「This session has not been saved yet」 | pi 要求会话先有 assistant 回复才能克隆 |
| 模型下拉为空 | 检查 pi 的 `models.json` / 渠道配置 |
| 远程设备返回 403 | 配对码缺失/过期，或该操作的能力开关未开——检查 **设置 → 访问模式** |
| 开发改动不生效 | 使用轮询 watcher 启动 dev（见 `scripts/dev.mjs` 的 `CHOKIDAR_USEPOLLING`） |
| 标签页显示旧会话 | 该会话被其他标签切换走了——重新激活该标签；每个标签激活时都会重载自己的会话 |

---

## 14. 开发

```bash
npm run dev          # 并行：vite(18384) + tsx watch server(3001)
npm run dev:web      # 仅 vite
npm run dev:server   # 仅 tsx watch server
npm run typecheck
npm run lint
npm test             # vitest — 共享 zod schema + 服务端会话解析
npm run build        # typecheck + lint + 打包到 dist/
npm run showcase:record  # 渲染演示走查视频（见 README）
```

目录：`src/`（React SPA）· `server/`（RPC 桥、REST、SSE）· `shared/`
（zod 边界 schema）· `public/`（PWA manifest、图标、Service Worker）。

面板另附多标签工作台的 headless 冒烟脚本：
`node scripts/smoke-tabs.mjs`（需要 Google Chrome / `CHROME_PATH`；
会自动从生产页面读取后端控制令牌）。

### 贡献
向仓库提交 issue/PR。项目为 Apache-2.0，独立编写（clean-room），
全部代码从零写出。

---

*PiHub — Apache License 2.0。字体：HarmonyOS Sans SC（华为）与 IBM Plex
（SIL OFL）；HM Symbols 子集来自 `hm_symbol` 包。*
